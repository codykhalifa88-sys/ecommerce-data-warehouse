# data/raw

Place the Olist CSVs here (see the main README's "Data Source" section), or run:

```bash
python scripts/generate_sample_data.py
```

to generate a small synthetic dataset shaped identically to the real CSVs, for
a first run without a Kaggle account. Everything except this file is
gitignored.
